package com.patterns.creational;

/**
 * Enum Singleton
 * + Witty
 * + Serialization
 * + Thread-safety
 * + supports 'switch' statement
 * + Ability to use EnumSet, EnumMap, etc
 * -  Not Lazy initialization
 */
public enum EnumSingleton {
    INSTANCE
}
